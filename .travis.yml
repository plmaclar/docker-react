language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t plmaclar/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true plmaclar/docker-react npm run test
  
